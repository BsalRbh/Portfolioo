# Copy to .env.local and fill in your values.
# Neon (or Vercel Postgres) connection string used by /app/api/leaderboard.
# Get this from the Neon dashboard → Connection Details → Pooled connection.
DATABASE_URL=postgres://USER:PASSWORD@HOST/DBNAME?sslmode=require
